apiVersion: v2
name: xboard
description: A Helm chart for deploying the XBoard panel (web + horizon + ws-server)
type: application
version: 0.1.0
appVersion: "latest"
kubeVersion: ">=1.29.0-0"
keywords:
  - xboard
  - laravel
  - v2board
  - panel
